# probably going to rename this step3-script eventually
#
# the lines are all commented out to prevent accidental overwrites of work in progress
# when you're sure you actually want to run the script, uncomment the lines, run it, and then reprotect

for s in {3..13}
do 
#   cp template-pe-map.json dn${s}_pe-map.json
#   cp template-pe-subst-en-sujato.json dn${s}_pe-subst-en-sujato.json
#   cp template-pe-subst-pli-ms.json dn${s}_pe-subst-pli-ms.json
#   cp ../bilara-data/translation/en/sujato/sutta/dn/dn${s}_translation-en-sujato.json dn${s}-anchors.json
done
